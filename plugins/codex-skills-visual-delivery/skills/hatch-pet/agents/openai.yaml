interface:
  display_name: "Hatch Pet Legacy v1"
  short_description: "Explicit repair for legacy v1 pets"
  default_prompt: "Use $hatch-pet to inspect and repair this existing v1 pet without migrating its format."
policy:
  allow_implicit_invocation: false
