interface:
  display_name: "PDF Legacy Adapter"
  short_description: "Explicit legacy PDF delivery adapter"
  icon_large: "./assets/pdf.png"
  default_prompt: "Use $pdf to select the official PDF workflow or an explicitly chosen legacy fallback."
policy:
  allow_implicit_invocation: false
